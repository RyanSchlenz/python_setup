# =============================================================================
# Development and code quality tools
# =============================================================================
# Install alongside requirements.txt for a complete dev environment:
#   pip install -r requirements.txt -r requirements-dev.txt

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Code formatting
black>=24.0.0

# Linting (fast, replaces pylint + flake8 + isort)
ruff>=0.4.0

# Type checking
mypy>=1.0.0

# Pre-commit hook framework
pre-commit>=3.0.0
